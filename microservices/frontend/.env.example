# Frontend Environment Variables

# API Configuration
# Production: Leave empty to use relative URLs (same origin)
# Development: Set to empty or your local API server
VITE_API_BASE_URL=http://localhost

# Development Proxy Target (only used in dev mode)
# This is the URL where Vite will proxy /api requests during development
VITE_API_PROXY_TARGET=http://localhost:30080

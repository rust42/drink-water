# Disable VFS file watching to prevent SIGSEGV crash in libnative-platform-file-events.so
# on Linux ARM64 (aarch64) — affects Docker builds on Apple Silicon Macs with Gradle 8.5
org.gradle.vfs.watch=false
